name: expense_tracker
description: SpendWise - an offline-first personal finance manager.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^1.1.1
  flutter_riverpod: ^3.0.3
  go_router: ^17.0.1
  hive_ce_flutter: ^2.3.4
  intl: ^0.20.2
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
